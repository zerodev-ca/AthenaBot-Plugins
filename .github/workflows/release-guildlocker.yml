name: Release GuildLocker

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}

    steps:
      - name: Checkout guildlocker branch
        uses: actions/checkout@v7
        with:
          ref: guildlocker

      - name: Read version from main.js
        id: ver
        run: |
          v=$(grep -oE "version:[[:space:]]*'[^']*'" main.js | head -1 | sed -E "s/.*'([^']*)'.*/\1/")
          if [ -z "$v" ]; then echo "::error::No version field found in main.js"; exit 1; fi
          echo "::notice::Releasing guildlocker $v"
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - name: Build ZIP
        run: |
          mkdir -p staging/guildlocker
          cp -r main.js src data staging/guildlocker
          cd staging
          zip -r ../guildlocker.zip guildlocker/

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: guildlocker
          path: guildlocker.zip

  release:
    name: Publish Release
    runs-on: ubuntu-latest
    needs: [build]

    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: guildlocker-${{ needs.build.outputs.version }}
          name: "GuildLocker - ${{ needs.build.outputs.version }}"
          body: |
            ## Description
            Download the plugin below!
            - `guildlocker.zip`
          files: |
            guildlocker.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
