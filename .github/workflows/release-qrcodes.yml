name: Release QRCodes

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}

    steps:
      - name: Checkout qrcodes branch
        uses: actions/checkout@v7
        with:
          ref: qrcodes

      - name: Read version from main.js
        id: ver
        run: |
          v=$(grep -oE "version:[[:space:]]*'[^']*'" main.js | head -1 | sed -E "s/.*'([^']*)'.*/\1/")
          if [ -z "$v" ]; then echo "::error::No version field found in main.js"; exit 1; fi
          echo "::notice::Releasing qrcodes $v"
          echo "version=$v" >> "$GITHUB_OUTPUT"

      - name: Build ZIP
        run: |
          mkdir -p staging/qrcodes
          cp -r main.js src data staging/qrcodes
          cd staging
          zip -r ../qrcodes.zip qrcodes/

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          name: qrcodes
          path: qrcodes.zip

  release:
    name: Publish Release
    runs-on: ubuntu-latest
    needs: [build]

    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: qrcodes-${{ needs.build.outputs.version }}
          name: "QRCodes - ${{ needs.build.outputs.version }}"
          body: |
            ## Description
            Download the plugin below!
            - `qrcodes.zip`
          files: |
            qrcodes.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
